{
  "name": "campus-inventory-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "predev": "prisma generate && prisma db push",
    "dev": "nodemon src/server.js",
    "prestart": "prisma generate && prisma db push",
    "start": "node src/server.js",
    "seed": "node scripts/seed-xlsx.js",
    "promote-admin": "node scripts/promote-admin.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "jsonwebtoken": "^9.0.2",
    "activedirectory2": "^2.2.0",
    "@prisma/client": "^5.7.1",
    "xlsx": "^0.18.5",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "prisma": "^5.7.1",
    "nodemon": "^3.0.2"
  }
}